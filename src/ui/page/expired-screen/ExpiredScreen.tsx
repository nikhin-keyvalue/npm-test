import Preact, { h } from 'preact';

import './style.scss';

const ExpiredScreen: Preact.FunctionComponent = () => <p className='zamp-expired-text'>SESSION EXPIRED</p>;

export default ExpiredScreen;
